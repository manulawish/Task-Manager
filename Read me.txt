//when you setup the project in your PC no need to run this command again
dotnet ef migrations add InitialCreate -c AppDbContext --project ./TaskManagementApp.Infrastructure --startup-project ./TaskManagementApp.API


// just run this command inside the TaskManagementApp.API folder to update or create the database in your local machine
// befor runnig this command make sure you have the corect connection string in the appsettings.json file
dotnet ef database update -c AppDbContext --startup-project ./TaskManagementApp.API --project ./TaskManagementApp.Infrastructure

// There are cuple of method to create a user in the database.
// 1 you can use the insert statement that is in the bottom of the DB Script
// 2 you can use the register endpoint in the  Swagger UI
